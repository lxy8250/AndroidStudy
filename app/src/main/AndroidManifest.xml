<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lxy.androidstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".customView.CoustomViewActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".anim.AnimatorActivity" />
        <activity android:name=".rxjava.FirstRxjavaActivity" />
        <activity android:name=".rxjava.DelayActivity" />
        <activity android:name=".rxjava.Rxjava2Activity" />
        <activity android:name=".rxjava.ChangeActivity" />
        <activity
            android:name=".appbar.AppBarActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".customView.ruler.RulerActivity"></activity>
        <activity android:name=".clickdispatch.ClickDispatchActivity"></activity>
        <activity
            android:name=".swipeback.SwipeBackActivity"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".handle.HandleActivity" />
    </application>

</manifest>